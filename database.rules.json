{
  "rules": {
    "users": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid"
      }
    },
    "responses": {
      ".read": "auth != null",
      ".write": true
    }
  }
}